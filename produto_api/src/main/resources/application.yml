spring:
  application:
    name: produto_api
  datasource:
    url: jdbc:h2:mem:produtos
    username: sa
    password: 123
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /produtos_db
